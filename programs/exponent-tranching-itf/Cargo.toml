[package]
name = "exponent-tranching-itf"
version = "0.1.0"
description = "Interface to the Exponent tranching program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "exponent_tranching_itf"

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
test-bpf = []
debug = []

[dependencies]
anchor-lang = "0.28.0"
